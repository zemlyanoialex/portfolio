rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;
      // Temporary seeding window. Revert to `false` after data import.
      allow write: if request.time < timestamp.date(2026, 3, 20);
    }
  }
}
